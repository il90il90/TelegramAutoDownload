<mah:MetroWindow
    x:Class="TelegramAutoDownload.MembersExportDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    Title="Export Members"
    Width="640" Height="520"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResizeWithGrip"
    ShowInTaskbar="False">

    <mah:MetroWindow.Resources>
        <Style x:Key="ActionBtn" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square.Accent}">
            <Setter Property="Padding"    Value="14,6"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="MinWidth"   Value="110"/>
        </Style>
        <Style x:Key="SecBtn" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
            <Setter Property="Padding"   Value="14,6"/>
            <Setter Property="FontSize"  Value="13"/>
            <Setter Property="MinWidth"  Value="90"/>
        </Style>
    </mah:MetroWindow.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="TxtTitle" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock x:Name="TxtStatus" FontSize="12" Foreground="#888" Margin="0,4,0,0" Text="Press 'Fetch Members' to start"/>
        </StackPanel>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="1" x:Name="PbProgress"
                     Height="6" Margin="0,0,0,10"
                     Minimum="0" Maximum="100" Value="0"
                     Foreground="#2196F3"/>

        <!-- Members list -->
        <DataGrid Grid.Row="2"
                  x:Name="DgMembers"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  FontSize="12"
                  Margin="0,0,0,10"
                  ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"          Binding="{Binding UserId}"    Width="80"/>
                <DataGridTextColumn Header="Username"   Binding="{Binding Username}"  Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding Username, StringFormat=@{0}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Last Name"  Binding="{Binding LastName}"  Width="120"/>
                <DataGridTextColumn Header="Phone"      Binding="{Binding Phone}"     Width="110"/>
                <DataGridCheckBoxColumn Header="Admin"  Binding="{Binding IsAdmin}"   Width="55"/>
                <DataGridCheckBoxColumn Header="Bot"    Binding="{Binding IsBot}"     Width="45"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Summary row -->
        <TextBlock Grid.Row="3" x:Name="TxtCount"
                   FontSize="12" Foreground="#666"
                   Margin="0,0,0,10"
                   Text="No members loaded yet."/>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="BtnFetch"
                    Content="⬇ Fetch Members"
                    Style="{StaticResource ActionBtn}"
                    Click="BtnFetch_Click"/>

            <Button Grid.Column="2" x:Name="BtnCsv"
                    Content="💾 Export CSV"
                    Style="{StaticResource SecBtn}"
                    IsEnabled="False"
                    Click="BtnCsv_Click"/>

            <Button Grid.Column="4" x:Name="BtnVcard"
                    Content="📇 Export vCard"
                    Style="{StaticResource SecBtn}"
                    IsEnabled="False"
                    Click="BtnVcard_Click"/>

            <Button Grid.Column="6"
                    Content="Close"
                    Style="{StaticResource SecBtn}"
                    IsCancel="True"
                    Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</mah:MetroWindow>
